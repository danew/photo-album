import styled from 'styled-components';

const List = styled.section`
  width: 100%;

  a, div {
    margin-bottom: 10px;

    &:last-child {
      margin-bottom: 0px;
    }
  }
`;

export default List;